function exampleFunction() {
  if (arguments.length > 0) {
    // Perform some action based on the number of arguments
    this._y = _; // Assign the value of _ to this._y
    return this;
  } else {
    return this._y;
  }
}
